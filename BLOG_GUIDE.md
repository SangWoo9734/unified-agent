# 블로그 작성 가이드: Multi-Product AI Agent 구축기

이 문서는 블로그 포스트 작성을 위한 자료입니다.

---

## 📝 블로그 포스트 아웃라인 제안

### 제목 후보
1. "두 개의 프로덕트, 하나의 AI 에이전트로 관리하기"
2. "마케팅 리소스를 어디에? 데이터 기반 의사결정 자동화"
3. "AdSense 수익 최적화: Multi-Product SEO Agent 구축기"

### 구조

#### 1. 도입부: 문제 정의
```
"QR Generator와 Convert Image, 두 개의 프로덕트를 운영하고 있다.
둘 다 트래픽이 중요한데, 어디에 시간을 더 투자해야 할까?"

현실:
- Convert Image에만 SEO 에이전트가 있었음
- QR Generator는 AdSense 광고로 수익화 중인데 데이터 분석 없이 운영
- 리소스 배분을 "감"으로 결정하고 있었음
```

#### 2. 핵심 질문
```
1. 두 프로덕트 모두 마케팅이 필요한가?
   → 증거: QR Generator에 AdSense가 달려있음 = 트래픽 의존적 비즈니스

2. 프로덕트를 비교할 필요가 있는가?
   → 증거: AdSense 수익은 트래픽에 비례, 잘못된 리소스 배분 = 기회비용 손실
```

#### 3. 설계 과정

**왜 모노레포가 아닌가?**
- 두 프로덕트는 코드를 공유하지 않음
- Next.js vs Vite로 기술 스택이 다름
- 단지 "데이터 분석"만 통합하면 됨

**선택한 구조:**
```
agent-product/
├── unified-agent/         # Python 통합 에이전트
├── convert-image/         # 독립 프로젝트
└── qr-generator/          # 독립 프로젝트
```

**핵심 설계 결정:**
1. 기존 코드 재사용 (convert-image/agent의 collectors)
2. 새로운 비교 분석 레이어 추가 (comparative_analyzer.py)
3. products.yaml로 프로덕트 설정 중앙화

#### 4. 구현 상세

**4.1 데이터 수집 통합**
```python
# 각 프로덕트별로:
- Google Search Console (검색 성과)
- Google Analytics 4 (사용자 행동)
- Google Trends (검색 트렌드)
- AdSense (광고 수익) ← QR Generator만
```

**4.2 비교 분석 엔진**

`comparative_analyzer.py`의 핵심:
```python
def analyze_products(products_data):
    # 1. 각 프로덕트 데이터 요약
    # 2. Claude에게 통합 분석 요청
    # 3. 리소스 배분 추천 생성
```

Claude에게 주는 프롬프트 구조:
- 트래픽 & 수익 비교
- 리소스 배분 추천 (SEO 에이전트 실행 빈도, 마케팅 예산)
- 교차 프로모션 기회
- 우선순위 액션 플랜

**4.3 AdSense 데이터 처리**

문제: AdSense Management API는 복잡함
해결: 환경변수로 수동 입력
```env
ADSENSE_REVENUE=18.50
ADSENSE_IMPRESSIONS=12450
ADSENSE_CLICKS=89
```

나중에 API 통합 가능하도록 설계

#### 5. 실제 사용 예시

**실행 전:**
```bash
python main.py
```

**출력:**
```
📊 QR Generator 데이터 수집 중...
  ✓ 12,450 클릭 수집
  ✓ AdSense: $18.50

📊 Convert Image 데이터 수집 중...
  ✓ 8,230 클릭 수집

🤖 Claude AI 통합 분석 중...
```

**생성된 리포트 예시:**
```markdown
## 리소스 배분 추천
- QR Generator: 주간 실행 (광고 수익 의존도 높음)
- Convert Image: 격주 실행 (리소스 절약)
- 마케팅 예산: QR 70%, Convert 30%

근거:
- QR의 RPM이 $1.49로 높음
- 트래픽 1000 증가 시 예상 수익 +$1.49
- Convert는 직접 수익 없음, SEO 투자 효율 낮음
```

#### 6. 얻은 인사이트

**데이터로 발견한 것들:**
- QR Generator가 실제로는 더 높은 우선순위였음 (AdSense 수익)
- Convert Image 사용자가 QR을 만들 가능성 발견 (교차 프로모션 기회)
- SEO 에이전트를 매주 돌릴 필요 없음 → 비용 절감

**비즈니스 임팩트:**
- Claude API 비용: 월 $0.80 (격주 실행)
- 절약된 시간: 주 2시간 (수동 분석 제거)
- 데이터 기반 의사결정으로 ROI 향상

#### 7. 교훈 & 다음 단계

**배운 점:**
1. "감"이 아닌 데이터로 리소스 배분
2. AdSense가 있다 = 마케팅 필수
3. 프로덕트 간 시너지는 데이터에서 발견됨

**개선 가능한 부분:**
- AdSense API 완전 통합
- Slack 알림 추가
- A/B 테스트 결과 자동 추적

**확장 가능성:**
- 3개, 4개 프로덕트로 확장 용이
- 다른 수익 모델 (유료 플랜 등) 추가 가능

---

## 💡 블로그 작성 팁

### 포함하면 좋은 요소

1. **Before/After 비교**
   - Before: Convert Image에만 에이전트, QR은 방치
   - After: 데이터 기반 리소스 배분

2. **코드 스니펫**
   - `comparative_analyzer.py`의 핵심 로직
   - `products.yaml` 설정 예시
   - Claude 프롬프트 구조

3. **실제 데이터**
   - 가상의 예시 수치 사용
   - "QR: 12,450 클릭 vs Convert: 8,230 클릭"

4. **다이어그램**
```
┌─────────────┐
│ QR Generator│───┐
└─────────────┘   │
                  ▼
┌─────────────┐   ┌──────────────┐   ┌─────────┐
│Convert Image│──▶│Unified Agent │──▶│ Claude  │
└─────────────┘   └──────────────┘   └─────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │비교 리포트   │
                  │리소스 배분   │
                  └──────────────┘
```

5. **의사결정 트리**
```
AdSense 있음?
├─ Yes → 트래픽 = 수익 → 마케팅 필수
└─ No  → 다른 수익 모델?
         ├─ Yes → 전환율 최적화
         └─ No  → 우선순위 낮음
```

### 독자 타겟

**주 타겟:**
- 여러 사이드 프로젝트를 운영하는 개발자
- 데이터 기반 의사결정에 관심 있는 사람
- SEO/마케팅 자동화를 고민하는 사람

**부 타겟:**
- AI 에이전트 구축에 관심 있는 사람
- Claude API 활용 사례를 찾는 사람

---

## 🎯 핵심 메시지

1. **"감"이 아닌 "데이터"로 리소스 배분하기**
2. **AdSense = 트래픽 의존 = 마케팅 필수**
3. **프로덕트 비교로 숨겨진 기회 발견**
4. **자동화로 시간 절약, 객관성 확보**

---

## 📊 사용 가능한 통계/수치

- Claude API 비용: 실행당 $0.10~0.20
- 절약 시간: 주 2시간 (수동 분석 제거)
- 프로덕트 수: 2개 → 확장 가능
- 데이터 소스: 4개 (GSC, GA4, Trends, AdSense)
- 리포트 길이: 평균 2,000 단어

---

## 🔗 참고 링크 (블로그에 포함 가능)

- Anthropic Claude API: https://docs.anthropic.com/
- Google Search Console API: https://developers.google.com/webmaster-tools
- Google Analytics Data API: https://developers.google.com/analytics/devguides/reporting/data/v1
- PyTrends (Google Trends): https://pypi.org/project/pytrends/

---

## 🖼️ 스크린샷 아이디어

1. `products.yaml` 설정 파일
2. 터미널 실행 화면 (데이터 수집 과정)
3. 생성된 리포트 일부 (마크다운)
4. 폴더 구조 (`tree` 명령 결과)
5. Claude 분석 결과 예시

---

## 📌 핵심 코드 하이라이트

### 1. products.yaml 구조
```yaml
products:
  qr-generator:
    name: "QR Studio"
    has_adsense: true          # 핵심 차별점
    priority: "high"           # 우선순위
```

### 2. 비교 분석 프롬프트
```python
prompt = f"""
당신은 여러 프로덕트를 운영하는 마케팅 분석가입니다.
{summary}

다음을 추천하세요:
1. 리소스 배분 (SEO 에이전트 실행 빈도)
2. 마케팅 예산 비율
3. 교차 프로모션 기회
"""
```

### 3. 데이터 수집 흐름
```python
for product_id, config in products.items():
    data = collect_product_data(product_id, config, credentials)
    all_data.append(data)

# Claude 분석
report = analyzer.analyze_products(all_data)
```

---

## ✍️ 블로그 작성 시 주의사항

1. **과장하지 말기**
   - "혁명적" 같은 단어 자제
   - 실제 경험 기반으로 작성

2. **코드는 핵심만**
   - 전체 코드 덤프 금지
   - 핵심 로직만 스니펫으로

3. **독자 입장에서**
   - "이걸 왜 만들었나?" 먼저 설명
   - "어떻게 만들었나?" 그 다음

4. **재현 가능하게**
   - GitHub 링크 제공 (선택사항)
   - 또는 핵심 파일만 첨부

---

## 🎨 제목 & 썸네일 아이디어

**제목:**
- "두 프로덕트를 데이터로 비교하는 AI 에이전트 만들기"
- "AdSense 수익 최적화: Multi-Product SEO Agent"
- "마케팅 리소스, 어디에 집중할까? AI가 답하다"

**썸네일 요소:**
- 두 개의 프로덕트 아이콘
- 화살표 → Claude 아이콘
- 📊 차트/그래프 이미지
- "vs" 또는 "⚖️" 저울 이미지

---

이 가이드를 참고해서 블로그 포스트를 작성하시면 됩니다!
핵심은 "문제 → 설계 → 구현 → 결과" 흐름을 명확히 하는 것입니다.

---
---

# 📚 Version 2: Level 2 Agent & spec-flow 도입기

**날짜**: 2026-01-11
**주제**: "분석만 하는 Agent"에서 "행동하는 Agent"로 진화

---

## 🎯 새로운 문제 인식

### Level 1 Agent의 한계

**현재 상태 (unified-agent v1.0)**:
```
데이터 수집 → 분석 → 리포트 생성 → 📋 여기서 끝
```

**문제점**:
```
리포트: "QR Generator의 메타 타이틀을 'Free QR Code Generator'로 변경하세요"
     ↓
사용자: 파일 찾기 → 수동 수정 → 커밋 → PR 생성 (30분 소요)
     ↓
결과: 바쁘면 실행 안 함 → 리포트만 쌓임 → 지표 개선 안 됨
```

**핵심 깨달음**:
> "리포트만 만드는 건 Agent가 아니다. 최소한 PR까지는 자동으로 만들어야 진짜 Agent다."

### AI Agent의 정의

**엄격한 정의**:
```
환경 감지 → 의사결정 → 액션 실행 → 결과 관찰 → 학습 → 반복
```

**unified-agent v1.0 현실**:
- ✅ 환경 감지 (데이터 수집)
- ✅ 의사결정 (Claude 분석)
- ❌ **액션 실행** ← 없음!
- ❌ 결과 관찰
- ❌ 학습

**결론**: Level 1은 "AI-Powered 분석 도구"이지, Agent가 아니다.

---

## 💡 Level 2 Agent 목표 정의

### 3단계 Agent 진화 로드맵

**Level 1: Analysis Agent (현재)**
```
데이터 → 리포트
```
→ 이건 그냥 "분석 스크립트"

**Level 2: Semi-Autonomous Agent (목표)**
```
데이터 → 리포트 → PR 자동 생성 → 사람이 리뷰/머지
```
→ 이제 "Act"가 있으니 Agent!

**Level 3: Autonomous Agent (미래)**
```
데이터 → 분석 → 자동 실행 → 결과 측정 → 학습 → 반복
```
→ 완전 자동, 하지만 여러 주 검증 필요

**의사결정**:
- Level 2까지는 **지금** 구현
- Level 2에서 리포트와 PR을 보고
- Level 3은 신뢰도 확보 후 고려

---

## 🛠️ spec-flow 도입 결정

### 왜 spec-flow인가?

**문제 인식**:
```
"Level 2를 구현하려면 복잡도가 높아진다"

필요한 것:
- 리포트 파싱
- 파일 변경 (메타 타이틀, 내부 링크)
- Git 작업 (브랜치, 커밋, 푸시)
- GitHub PR 생성
- 에러 처리 및 롤백
```

**기존 방식의 문제**:
- 바로 코드부터 짜면 → 설계 누락 → 리팩토링 지옥
- "감"으로 구현하면 → 안전장치 부족 → 프로덕션 코드 망가뜨림

**spec-flow 선택 이유**:
1. **What/Why 먼저, How 나중**: 요구사항 명확화
2. **체계적 설계**: spec.md → plan.md → tasks.md → TODOs
3. **안전성 확보**: 문서로 리스크 사전 식별
4. **확장 가능**: 나중에 기능 추가할 때도 재사용

**spec-flow란?**
- Spec-Kit (GitHub) + BMAD (멀티에이전트 검증) 결합
- Claude Code의 커스텀 워크플로우
- 5단계: Analysis → Planning → Solutioning → Implementation → Summary

---

## 📖 spec-flow 워크플로우 실행 과정

### Phase 1: Analysis (요구사항 명확화)

**핵심 질문**:
1. Level 2 Agent의 **진짜 목표**는 뭔가?
   - 답: 리포트 → PR 자동 생성 → 지표 개선 속도 향상

2. 어떤 액션을 자동화할 것인가?
   - 안전한 것: 메타 타이틀/설명, 내부 링크
   - 위험한 것: JavaScript 로직, API 엔드포인트 (제외)

3. 실패하면 어떻게 할 것인가?
   - 백업 + 롤백 필수
   - 프로덕트별 독립 실행 (하나 실패해도 다른 건 계속)

### Phase 2: Planning (기술 설계)

**Architecture 탐색**:
```
Explore Agent 실행:
- "GitHub PR 자동화 방법"
- "Python에서 안전한 파일 수정"
- "Git 롤백 전략"

발견한 것:
- PyGithub (GitHub API)
- GitPython (Git 자동화)
- LibCST (TypeScript AST 파싱, 포매팅 유지)
- BeautifulSoup (HTML 파싱)
- Context Manager 패턴 (자동 롤백)
```

**Plan Agent 실행**:
```
입력: 배경, 목표, 제약사항
출력: 30KB 분량의 상세 구현 계획

포함된 내용:
- Architecture 다이어그램
- 7개 핵심 클래스 설계
- 데이터 모델 (Action, ExecutionResult)
- 에러 처리 전략
- 보안 위협 모델
- 테스트 전략
- 5주 마이그레이션 플랜
```

### Phase 3: Solutioning (검증)

**원래 BMAD 프로세스**:
```
PAL MCP consensus:
- Model 1 (for): Product Manager 관점
- Model 2 (against): Architect 관점
- Model 3 (neutral): QA 관점

3개 모델로 설계 검증
```

**우리의 선택**:
> "초기 트래픽이 적어서 spec/plan 단위가 작다.
> 사람 개입 단계를 늘리지 않아도 된다."
> → PAL consensus 건너뛰고 바로 구현 단계로

### Phase 4: Implementation Prep (TODO 분해)

**tasks.md 생성**:
- 15개 Task로 분해
- Dependency Graph 작성
- Week별 실행 계획
- Critical Path 식별

**TODO 파일 생성**:
```
todos/
├── 01-priority-high/
│   ├── 01-setup-dependencies.md      (30분)
│   ├── 02-core-data-classes.md       (20분)
│   ├── 07-meta-updater.md            (3시간, 가장 복잡)
│   └── ...
├── 02-priority-medium/
└── 03-priority-low/
```

**각 TODO 파일 구조**:
```markdown
# TODO-XX: 제목

## Business Value
- Goal: 비즈니스 목표
- Impact: 왜 중요한가
- Why This Matters: 맥락

## Tasks
- [ ] 구체적 작업 1
- [ ] 구체적 작업 2

## Acceptance Criteria
- [ ] 완료 조건

## Technical Notes
- 구현 힌트, Gotchas

## Related Files
- 수정할 파일 목록
```

---

## 📊 생성된 산출물

### 1. [spec.md](specs/pr-automation/spec.md) (23KB)

**What & Why 문서**

**포함 내용**:
- Problem Statement: Level 1의 한계, Level 2가 필요한 이유
- User Stories (5개):
  - US-1: 자동 메타 타이틀 변경
  - US-2: 자동 내부 링크 추가
  - US-3: 안전한 롤백
  - US-4: 선택적 자동화
  - US-5: PR 리뷰 편의성
- Functional Requirements (8개):
  - FR-1: 액션 추출 (리포트 파싱)
  - FR-2: 액션 검증 (안전성 필터)
  - FR-3: 파일 변경 실행
  - FR-4: PR 생성
  - FR-5: 에러 처리 및 롤백
- Acceptance Criteria: Given-When-Then 형식 5개
- Risks & Mitigations: 5개 주요 리스크
- Success Metrics: 실행률, PR 성공률, 리뷰 통과율

**블로그 활용**:
- "왜 Level 2가 필요했는가?" 섹션에 직접 인용
- User Stories → 구체적 시나리오로 활용

### 2. [plan.md](specs/pr-automation/plan.md) (31KB)

**How 문서**

**포함 내용**:
- Architecture Overview:
  ```
  ActionExtractor → ActionValidator → ActionExecutor → PRCreator
  ```
- Component Design (7개 핵심 클래스):
  - ActionExtractor: 리포트 파싱
  - ActionValidator: 안전성 검증
  - MetaUpdater: TSX/HTML 메타 변경 (LibCST, BeautifulSoup)
  - LinkInjector: 내부 링크 추가
  - PRCreator: Git + GitHub PR
  - FileBackupManager: 백업/롤백
  - Level2Agent: Orchestrator
- Data Model: Action, ExecutionResult dataclass
- Security: Threat Model (XSS, Path Traversal, Code Injection)
- Testing Strategy: Unit, Integration, E2E
- Migration Plan: 5주, 5 Phases

**블로그 활용**:
- Architecture 다이어그램 그대로 사용
- MetaUpdater 로직 → 코드 스니펫으로
- Security 섹션 → "어떻게 안전하게 만들었나" 설명

### 3. [tasks.md](specs/pr-automation/tasks.md) (21KB)

**구현 분해 문서**

**포함 내용**:
- 15개 Task 상세 설명
- Dependency Graph (mermaid)
- Week별 실행 순서:
  - Week 1: Infrastructure (Setup, Data Classes, FileBackup)
  - Week 2: Core Logic (Extractor, Validator, MetaUpdater, LinkInjector)
  - Week 3: Integration (PRCreator, Level2Agent, main.py)
  - Week 4: Testing & Docs
- Definition of Done 체크리스트

**블로그 활용**:
- "얼마나 걸렸나?" 섹션에 활용
- Dependency Graph → 구현 복잡도 시각화

### 4. [TODO Files](todos/) (3개 샘플)

**실행 단위 문서**

**생성된 파일**:
- [01-setup-dependencies.md](todos/01-priority-high/01-setup-dependencies.md)
- [02-core-data-classes.md](todos/01-priority-high/02-core-data-classes.md)
- [07-meta-updater.md](todos/01-priority-high/07-meta-updater.md) (가장 복잡, 3시간 예상)

**각 TODO 구조**:
- Business Value: 왜 이걸 하는가
- Tasks: 체크박스 형식 실행 목록
- Acceptance Criteria: 완료 조건
- Technical Notes: 구현 힌트, Gotchas
- Related Files: 수정할 파일

**블로그 활용**:
- "실제 구현은 어땠나?" 섹션
- TODO-07 (MetaUpdater) → 가장 복잡한 부분 설명

### 5. [todos/README.md](todos/README.md)

**전체 진행 상황 추적**

**포함 내용**:
- 15개 Task 목록 + 상태
- Critical Path 다이어그램
- Week별 Progress Tracking
- Estimated Time: 총 20시간 (4주, 주 5시간)

---

## 🎨 블로그 포스트 확장 아웃라인

### 새로운 섹션: "Level 2로 진화하기"

#### 8. Level 1의 한계 인식
```
"리포트는 잘 만들어지는데, 실행이 안 된다"

통계:
- 생성된 리포트: 10개
- 실제 실행된 액션: 5개 (50%)
- 이유: 바쁨, 수동 작업 번거로움

깨달음:
→ "분석만 하는 건 Agent가 아니다"
```

#### 9. spec-flow 도입 결정
```
"복잡한 기능은 설계부터 제대로 하자"

선택지:
A. 바로 코딩 → 빠르지만 위험
B. spec-flow → 느리지만 안전

선택: B (프로덕션 코드 보호가 우선)

spec-flow 프로세스:
1. spec.md 작성 (What & Why)
2. plan.md 작성 (How)
3. tasks.md 작성 (구현 분해)
4. TODO 파일 생성 (실행 단위)
```

#### 10. 설계 과정에서 발견한 것들
```
**Architecture 탐색 결과**:
- LibCST: TSX 파일 안전하게 수정 (포매팅 유지)
- Context Manager: 자동 롤백 보장
- PyGithub + GitPython: PR 자동화

**설계 단계에서 막은 위험**:
- XSS: ActionValidator로 필터링
- Path Traversal: 화이트리스트 검증
- Code Injection: LibCST/BeautifulSoup 사용

→ "설계 없이 바로 짰으면 이거 다 놓쳤을 것"
```

#### 11. 구현 복잡도
```
**총 작업량**:
- 15개 Task
- 20시간 예상
- 4주 계획

**가장 복잡한 부분**:
- MetaUpdater (3시간):
  - LibCST로 TSX 파싱
  - metadata 객체 찾기
  - 포매팅 유지하며 변경
  - 구문 검증

**Critical Path**:
Setup → Data → Extractor → MetaUpdater → PRCreator → Integration
```

#### 12. spec-flow의 가치
```
**Before spec-flow**:
- 머릿속으로만 설계
- 코드 짜다가 막히면 리팩토링
- 안전장치 나중에 추가

**After spec-flow**:
- 문서로 설계 공유 가능
- 리스크 사전 식별
- 안전장치가 설계에 포함됨
- TODO로 진행 상황 추적

**투자 대비 효과**:
- 설계 시간: +4시간
- 절약된 리팩토링 시간: -10시간
- 순이익: 6시간 절약 + 품질 향상
```

---

## 💻 코드 스니펫 추가

### spec-flow로 생성된 Architecture

```python
# Level2Agent Orchestrator
class Level2Agent:
    def __init__(self, api_key, github_token, workspace, config):
        self.extractor = ActionExtractor(api_key)
        self.validator = ActionValidator()
        self.executors = {
            'update_meta_title': MetaUpdater(workspace),
            'add_internal_link': LinkInjector(workspace),
        }
        self.pr_creator = PRCreator(github_token)

    def process_report(self, report_path):
        # 1. 액션 추출
        actions = self.extractor.extract_from_report(report_path)

        # 2. 검증
        valid_actions = [a for a in actions if self.validator.validate(a)[0]]

        # 3. 실행
        for action in valid_actions:
            executor = self.executors[action.action_type]
            result = executor.execute(action)

        # 4. PR 생성
        pr_url = self.pr_creator.create_pr(...)

        return pr_url
```

### LibCST로 안전한 TSX 변경

```python
import libcst as cst

class MetadataTransformer(cst.CSTTransformer):
    def __init__(self, new_title, new_description):
        self.new_title = new_title
        self.new_description = new_description

    def leave_Assign(self, original_node, updated_node):
        # metadata 객체 찾기
        if self._is_metadata_export(updated_node):
            # title, description 변경 (포매팅 유지)
            return self._update_metadata(updated_node)
        return updated_node

# 사용
module = cst.parse_module(tsx_code)
modified = module.visit(MetadataTransformer("New Title", "New Desc"))
```

### Context Manager로 안전한 Git 작업

```python
@contextmanager
def safe_git_operation(repo_path):
    repo = git.Repo(repo_path)
    original_branch = repo.active_branch.name

    try:
        yield repo
    except Exception as e:
        # 실패 시 자동 롤백
        repo.heads[original_branch].checkout()
        repo.git.reset('--hard')
        raise
    finally:
        # 항상 원래 브랜치로
        repo.heads[original_branch].checkout()

# 사용
with safe_git_operation('/path/to/repo') as repo:
    # Git 작업
    # 에러 발생 시 자동으로 롤백됨
```

---

## 📈 Before/After 비교 업데이트

### Level 1 (Before)
```
사용자: "리포트 보니까 메타 타이틀 바꾸래"
     ↓ (30분 수동 작업)
사용자: VSCode 열기 → 파일 찾기 → 수정 → 커밋 → PR 생성
     ↓
결과: 1주일에 2-3개 액션만 실행
```

### Level 2 (After)
```
Agent: "리포트 분석 → 액션 추출 → 파일 변경 → PR 생성"
     ↓ (30초 자동)
GitHub: PR #123 생성됨
     ↓
사용자: 리뷰 (2분) → Approve → Merge
     ↓
결과: 1주일에 10-15개 액션 실행 (5배 증가)
```

**지표 개선 속도**:
- Before: 리포트 → 실행까지 평균 3일
- After: 리포트 → PR 즉시, 머지까지 1일
- **총 2일 단축**

---

## 🎯 핵심 메시지 업데이트

### 기존 메시지
1. "감"이 아닌 "데이터"로 리소스 배분
2. AdSense = 트래픽 의존 = 마케팅 필수
3. 프로덕트 비교로 숨겨진 기회 발견
4. 자동화로 시간 절약, 객관성 확보

### 추가 메시지
5. **"분석만 하는 건 Agent가 아니다 - 액션까지 자동화하기"**
6. **"복잡한 기능은 spec-flow로 설계부터 제대로"**
7. **"안전장치(백업, 롤백)는 설계 단계에서 넣어야 한다"**
8. **"문서화된 설계는 나중에 기능 추가할 때도 재사용 가능"**

---

## 🖼️ 추가 다이어그램

### Level 2 Agent Architecture
```
┌─────────────────────────────────────────────┐
│          unified-agent (main.py)            │
├─────────────────────────────────────────────┤
│                                             │
│  Level 1: 데이터 수집 + 분석                 │
│  ├─ GSC/GA4/Trends/AdSense                  │
│  ├─ ComparativeAnalyzer                     │
│  └─ Report 생성 (markdown)                  │
│                                             │
│  ↓ (NEW)                                    │
│                                             │
│  Level 2: PR 자동화                         │
│  ├─ ActionExtractor (리포트 파싱)            │
│  ├─ ActionValidator (안전성 검증)            │
│  ├─ ActionExecutor                          │
│  │   ├─ MetaUpdater (LibCST)               │
│  │   └─ LinkInjector                       │
│  ├─ PRCreator (Git + GitHub)               │
│  └─ FileBackupManager (롤백)                │
│                                             │
└─────────────────────────────────────────────┘
         ↓
    GitHub PR 생성
         ↓
    사용자 리뷰/머지
```

### spec-flow Workflow
```
Phase 1: Analysis
  ├─ 요구사항 수집
  ├─ 모호함 해결
  └─ 핵심 질문 답변
      ↓
Phase 2: Planning
  ├─ spec.md (What & Why)
  ├─ plan.md (How)
  └─ 일관성 검증
      ↓
Phase 3: Solutioning
  ├─ PAL consensus (우리는 생략)
  └─ 설계 검증
      ↓
Phase 4: Implementation
  ├─ tasks.md (분해)
  └─ TODO 파일 생성
      ↓
Phase 5: Summary
  └─ 산출물 확인
```

---

## 🔗 추가 참고 링크

**spec-flow 관련**:
- Spec-Kit (GitHub): https://github.com/github/spec-kit
- BMAD-METHOD: https://github.com/bmad-code-org/BMAD-METHOD

**기술 스택**:
- LibCST: https://libcst.readthedocs.io/
- PyGithub: https://pygithub.readthedocs.io/
- GitPython: https://gitpython.readthedocs.io/
- BeautifulSoup: https://www.crummy.com/software/BeautifulSoup/

---

## ✍️ 블로그 작성 팁 (Version 2)

### 두 개의 포스트로 분리 가능

**포스트 1: "Multi-Product AI Agent 구축기"**
- 기존 내용 (Level 1)
- 문제 정의, 설계, 구현, 결과

**포스트 2: "Level 2 Agent로 진화: spec-flow 활용기"**
- Level 1의 한계
- spec-flow 도입
- 설계 → 구현 과정
- Before/After 비교

### 또는 하나의 긴 포스트

**구조**:
1. Part 1: Multi-Product Agent (Level 1)
2. Part 2: Level 2로 진화하기
3. 결론: Agent의 3단계 진화

---

## 📊 추가 통계

**spec-flow 산출물**:
- spec.md: 23KB, 11개 섹션
- plan.md: 31KB, 12개 섹션
- tasks.md: 21KB, 15개 Task
- TODO files: 3개 샘플 (나머지는 필요 시 생성)
- 총 문서량: ~75KB

**예상 구현 시간**:
- 설계 (spec-flow): 4시간
- 구현: 20시간
- 테스트: 3시간
- 총: 27시간 (약 1개월, 주 7시간)

**투자 대비 효과**:
- 설계 없이: 30시간 (리팩토링 포함)
- spec-flow 사용: 27시간
- 절약: 3시간 + 품질 향상 + 재사용 가능한 문서

---

## 🎨 제목 후보 (Version 2)

**단일 포스트 (전체)**:
1. "분석하는 Agent에서 행동하는 Agent로: Multi-Product AI Agent 진화기"
2. "Level 1→2→3: AI Agent를 단계별로 키우는 법"
3. "리포트만 보던 AI가 PR까지 만들기까지: spec-flow로 안전하게 진화하기"

**분리 포스트 (Part 2만)**:
1. "spec-flow로 Level 2 Agent 설계하기: 문서 먼저, 코드는 나중에"
2. "복잡한 기능은 설계부터: PR 자동화를 spec-flow로 구현한 이야기"
3. "안전한 AI Agent 만들기: LibCST, Context Manager, spec-flow"

---

**Updated**: 2026-01-11
**Version**: 2.0 (Level 2 Agent & spec-flow 추가)

이제 블로그에 Level 1뿐만 아니라 Level 2로 진화하는 과정까지 포함할 수 있습니다!
